#!/usr/bin/env bash
SCRIPT_DIR=$(dirname "$(readlink -f "$0")")

pushd "$SCRIPT_DIR/.." >/dev/null
latexmk -silent -c "$@"

# Uncomment to remove files ignored by .gitignore
# git clean -fdX

# Uncomment to remove pdf on clean
# rm my-thesis.pdf

popd >/dev/null
